{
  "devDependencies": {
    "@glennsl/bs-jest": "^0.5.1",
    "bs-platform": "^7.3.2",
    "bsb-js": "^1.1.7"
  },
  "dependencies": {
    "bs-webapi": "^0.15.9",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "reason-react": "^0.8.0"
  },
  "jest": {
    "testRegex": ".*_test.bs.js"
  },
  "scripts": {
    "ci": "yarn install && bsb -make-world && yarn test",
    "test": "bsb -make-world && jest",
    "generate-levels": "bsb && node ./src/Level_Generator.bs.js && bsb",
    "gen": "yarn generate-levels"
  }
}
