{
  "devDependencies": {
    "@glennsl/bs-jest": "^0.5.1",
    "bs-platform": "^7.2.2",
    "bsb-js": "^1.1.7"
  },
  "dependencies": {
    "bs-webapi": "^0.15.9",
    "reason-react": "0.7.1"
  },
  "jest": {
    "testRegex": ".*_test.bs.js"
  },
  "scripts": {
    "ci": "yarn install && yarn test && yarn generate-levels",
    "test": "bsb -make-world && jest",
    "generate-levels": "bsb && node ./src/Level_Generator.bs.js && bsb",
    "gen": "yarn generate-levels"
  }
}
